import Form from '@/app/ui/create-product-form';

export default async function Page(){
    return(
        <main>
            <Form id='1'/>
            {/* we need to also send the real seller id here */}
        </main>
    )
}